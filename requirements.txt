# Python dependencies for Earth View Wallpaper Engine v2.0
#
# Core dependencies (install via pip or apt):
requests>=2.28.0
beautifulsoup4>=4.11.0
lxml>=4.9.0

# System dependencies (install via apt - cannot be installed via pip):
# sudo apt install python3-gi python3-gi-cairo gir1.2-gtk-3.0 \
#   gir1.2-ayatanaappindicator3-0.1 gir1.2-notify-0.7 python3-cairo

# Optional: For better Unsplash integration
# Get a free API key at https://unsplash.com/developers

# Optional: For better NASA APOD access (higher rate limits)
# Get a free API key at https://api.nasa.gov
