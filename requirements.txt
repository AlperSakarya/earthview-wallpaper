# Python dependencies for Earth View Wallpaper Changer
requests>=2.28.0
# Note: For system packages, use apt install instead of pip:
# sudo apt install python3-cairo python3-gi python3-gi-cairo python3-bs4 python3-lxml

# System dependencies (install via apt):
# - gir1.2-gtk-3.0
# - gir1.2-ayatanaappindicator3-0.1
# - gir1.2-notify-0.7
